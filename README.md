# nucleo_trimmer
This repository includes the program and file used to test the code. The program was written to trim each given sequenced within a FASTQ file at a cut off point entered by the user. Restrictions included using a sliding window approach and organizing the functions created within a main() function.
